package com.rainyseason.cj.widget.watch

enum class WatchOnboardFeature(
    val featureName: String,
    val viewId: String,
) {
    FullSize(
        featureName = "full_size_watch_widget",
        viewId = "setting_full_size"
    )
}
